// console.log(name);
// var name = "John"; // variable declaration and initialization
// console.log(name);

console.log(a);
let a = "John"; // variable declaration and initialization
console.log(a);



// In JavaScript, variable declarations using var are hoisted to the top of their scope. 
// This means that the declaration of the variable is processed before any code is executed, 
// but the initialization (assignment of a value) is not hoisted. 
// Therefore, when we try to access the variable before it is initialized, it will return undefined instead of throwing an error.