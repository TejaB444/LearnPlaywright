

let age = 20;

let do_i_goto_goa= age>=18 ? "Yes" : "No";

console.log(do_i_goto_goa); // Output: "Yes"

//ternary operator is a shorthand for an if-else statement. 
// It takes three operands: a condition, a value to return if the condition is true, and a value to return if the condition is false. In this example, the condition is age>=18, the value to return if the condition is true is "Yes", and the value to return if the condition is false is "No".
// synatax condition ? value_if_true : value_if_false

let a =11;

let result = a%2==0 ? ++a : --a;
console.log(result); // Output: 10