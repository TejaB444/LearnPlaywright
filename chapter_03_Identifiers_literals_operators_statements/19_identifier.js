// let class="Hello";

// let if=10;

// let return=10;

let undefined=5; //works but not recommended as it can lead to confusion and bugs in the code. It is best to avoid using reserved keywords as variable names.
let infinity=10; //works but not recommended as it can lead to confusion and bugs in the code. It is best to avoid using reserved keywords as variable names.